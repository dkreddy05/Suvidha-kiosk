# Local development secrets (DO NOT commit a real .env)
POSTGRES_PASSWORD=change-me
REDIS_PASSWORD=change-me
PGADMIN_DEFAULT_PASSWORD=change-me

# If you run the auth service locally
MYSQL_PASSWORD=change-me

# JWT secret used by auth service (optional: auth has a safe default)
JWT_SECRET=change-me
